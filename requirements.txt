# VNPT AI Hackathon - Minimal Dependencies
# Optimized for Docker image size

# Core libraries
requests==2.31.0          # HTTP client for APIs (lightweight)
numpy==1.24.3            # Numerical operations (required for embeddings)

# Vector database (choose ONE)
faiss-cpu==1.7.4         # Fast similarity search (CPU only, smaller size)
# OR chromadb==0.4.15    # Alternative (heavier, has more deps)

# Data processing
pandas==2.0.3            # CSV I/O (minimal)

# Optional but recommended
wikipedia-api==0.6.0     # For data collection (not needed in inference)
beautifulsoup4==4.12.2   # For web scraping (not needed in inference)
lxml==4.9.3             # XML parser for BS4 (not needed in inference)

# Utils
tqdm==4.66.1            # Progress bars (optional, can remove for production)

# NO heavy dependencies:
# ❌ torch/tensorflow - not needed
# ❌ transformers - not needed (using API)
# ❌ langchain - too heavy, will implement lightweight RAG ourselves
# ❌ llama-index - too heavy
# ❌ sentence-transformers - using API for embeddings

# Production-only (remove development tools):
# - pytest
# - jupyter
# - matplotlib
# - etc.
